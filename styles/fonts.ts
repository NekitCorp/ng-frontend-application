import { Cinzel, Lato } from '@next/font/google';

export const cinzel = Cinzel({ subsets: ['latin'], weight: ['700'], display: 'swap' });
export const lato = Lato({ subsets: ['latin'], weight: ['400'], display: 'swap' });
